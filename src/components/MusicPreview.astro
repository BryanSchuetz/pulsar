---
import { getEntry } from 'astro:content';
const music = await getEntry('music', 'recent-albums');
const albums = music?.data?.slice(0, 8) ?? [];
---

<div class="bg-white py-8 sm:py-12 lg:py-14" id="music">
  <div class="mx-auto max-w-7xl px-5 sm:px-6 lg:px-8">
    <p class="mx-auto max-w-2xl text-left text-xl font-semibold leading-7 text-SignalPurp sm:text-2xl md:text-3xl lg:max-w-none">Tunes</p>
    <div class="mx-auto grid max-w-2xl border-t border-gray-200 pt-4 lg:mx-0 lg:max-w-none ">

      <p class="mb-12 text-lg leading-8 prose sm:mb-16 sm:text-sm">A grid of albums currently in 'Heavy Rotation'—generated programatically from my Music account. *caveat: the kids use my account on the Sonos all the time—so the data is hopelessly skewed, though still accurate. I'm probably listening to one of these albums right now.</p>
      <ul role="list" class="grid grid-cols-2 gap-x-4 gap-y-8 sm:grid-cols-3 sm:gap-x-6 lg:grid-cols-4 xl:gap-x-8">
      { albums.map((album) =>(
        <li class="relative">
          <div class="group aspect-h-10 aspect-w-10 block w-full overflow-hidden rounded-lg bg-gray-100 focus-within:ring-2 focus-within:ring-indigo-500 focus-within:ring-offset-2 focus-within:ring-offset-gray-100">
            <img src={album.image} alt={`Album art for ${album.album} by ${album.artist}`} class="pointer-events-none object-cover group-hover:opacity-75" loading="eager" decoding="async">
            <a href={album.link} type="button" class="plain absolute inset-0 focus:outline-none album">
              <span class="sr-only">{album.album}</span>
            </a>
          </div>
          <p class="pointer-events-none mt-2 block truncate text-sm font-medium text-gray-900">{album.album}</p>
          <p class="pointer-events-none block text-sm font-medium text-gray-500">{album.artist}</p>
        </li>
      ))}
      </ul>

    </div>
  </div>
</div>
